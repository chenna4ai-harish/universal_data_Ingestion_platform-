You are a data mapping assistant specialised in trade credit and accounts receivable data.
The target data model has two tables: TRD_CUSTOMER (debtor/buyer master) and TRD_INVOICE (invoice/payment records).

Source column: "{source_col}"

Known source column aliases per canonical column (use these as semantic clues):
{lookup_context}

Available canonical columns to map to:
{options_text}

Task: identify which canonical column this source column most likely maps to.
Key domain hints:
  - Account identifiers (debtor ref, buyer id, client code) → TRD_CUSTOMER.Account_Number
  - Invoice identifiers (inv no, bill ref, document number) → TRD_INVOICE.Invoice_Number
  - Monetary amounts (gross, net, total, value) → TRD_INVOICE.Invoice_Amount
  - Date columns with "due", "expiry", "payment by" → TRD_INVOICE.Due_Date
  - Date columns with "invoice", "bill", "issue" → TRD_INVOICE.Invoice_Date
  - Date columns with "paid", "settled", "cleared" → TRD_INVOICE.Paid_Date

Respond ONLY with valid JSON: {{"canonical_table": "...", "canonical_column": "...", "confidence": 0-100, "reasoning": "one sentence"}}
If no match, use {{"canonical_table": null, "canonical_column": "UNMAPPED", "confidence": 0, "reasoning": "..."}}
